#include <stdio.h>

	// every C program needs a main function - this is where the code starts running
	// a semi-colon is required at the end of each line
	// you don't actually need to return zero
	// the return type of this function is an integer

int main()
{
 	printf("hello, world!\n");
 	return 0;
}